import { FeaturePlaceholder } from '@/components/common/FeaturePlaceholder';
import { AppScreen } from '@/components/ui/AppScreen';

export function OrganizerMissionDetailScreen() {
  return (
    <AppScreen>
      <FeaturePlaceholder
        title='Organizer Mission Detail'
        description='Panel del organizador para monitorear avances, voluntarios y entregas de la mision.'
      />
    </AppScreen>
  );
}